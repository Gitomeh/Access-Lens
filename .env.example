# Server-side Gemini API key used by the /api/explain serverless function.
# It is NEVER exposed to the browser — do not rename it to VITE_GEMINI_API_KEY.
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
